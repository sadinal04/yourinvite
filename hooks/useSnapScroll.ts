"use client";

// ── MIGRATED TO NATIVE CSS SCROLL SNAPPING ──
// Custom JS scroll snapping logic has been disabled.
// We rely purely on CSS `scroll-snap-type: y mandatory` in globals.css.

export function useSnapScroll(_enabled: boolean) {
  return;
}
